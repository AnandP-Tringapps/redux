import React,{useState} from "react";
import redux from 'redux';
import { ReactDOM } from "react";
import { useSelector} from "@mui/base";



function Content({name, name1}){

    return(
        <div id="maincontent">
            <p>Content-1</p>
            
        </div>
    )
}

export default Content;